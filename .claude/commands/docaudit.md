---
description: Deep audit of the whole doc corpus for drift — deletes and restructures
---

Invoke the `docaudit` skill and follow it.

This is the occasional deep pass, not the end-of-session one. It reads every
doc at once and deletes: layered corrections, fused bullets, misfiled content,
stale facts, and resolved WATCHLIST items. Finding nothing wrong is a valid
result. Do NOT commit.
